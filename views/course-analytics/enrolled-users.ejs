<!--<link href="/course-analytics/user_list_box.css" rel="stylesheet" type="text/css" />-->
<div class="box-body no-padding" ng-controller="UserListEnrolledController">
    <ul class="products-list products-list-in-box">
        <li class="item" ng-repeat="user in userList">
            <div class="product-img"><img class="online img-circle" src="{{user.user.image}}" alt=""/></div>
            <div class="product-info">
                <span class="product-title">{{user.user.displayName}}</span>
                <span class="product-description">Enrolled: {{user.dateUpdated | date : 'dd.MMM.yyyy HH:mm:ss'}}</span>
            </div>
        </li>
    </ul>
    <!--<div class="user-list-box">
        <div class="item" ng-repeat="user in userList">
            <img class="online img-circle" src="{{user.user.image}}" alt=""/>
            <span class="message">
                <span class="name">{{user.user.displayName}}</span>
            </span>

        </div>
    </div>-->
</div>
